<ul class="icons alt">
    {% for link in links %}
        <li><a href="{{ link.url }}" class="icon alt fa-{{ link.faIcon }}"><span class="label">{{ link.title }}</span></a></li>
    {% endfor %}
</ul>